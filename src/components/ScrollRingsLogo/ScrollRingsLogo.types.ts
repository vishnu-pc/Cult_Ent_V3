/**
 * Props for the ScrollRingsLogo component.
 * This interface currently serves as a placeholder and can be extended
 * with additional props as the component evolves.
 */
export interface ScrollRingsLogoProps {
  className?: string;
}
