/**
 * Props for the CombinedWellnessSection component.
 * Currently no props are needed as this is a pure wrapper component.
 */

export interface CombinedWellnessSectionProps {
  // No props needed for the wrapper component
}
